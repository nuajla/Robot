# Web-app dependencies (install with: pip install -r requirements.txt)
# NOTE: rospy, cv_bridge and robotblockset are NOT listed here -- they come
# from the ROS Noetic / conda "franka" environment already set up on the
# robot-control PC (see README.md), and are only imported lazily by robot.py
# when capture.source=ros_topic or robot.enabled=true.
#
# numpy is pinned <2.0: ROS Noetic's system cv_bridge ships a compiled
# extension built against the pre-2.0 NumPy C-API. If a NumPy 2.x is loaded
# in the same process instead, cv_bridge's imgmsg_to_cv2() dies with
# "AttributeError: _ARRAY_API not found" immediately followed by a
# segfault. Do not remove this upper bound on the robot PC.
Flask>=3.0
PyYAML>=6.0
numpy>=1.24,<2.0
opencv-python>=4.8
huggingface_hub>=0.23
python-dotenv>=1.0
